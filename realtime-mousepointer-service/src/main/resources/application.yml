spring:
  application:
    name: realtime-websocket-microservice
server:
  port: 8000
eureka:
   instance:
       instance-id: "${spring.application.name}-${server.port}"
       preferIpAddress: true
   client:
      serviceUrl:
         defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}	